# AGENTS.md — 全AIコーディングエージェント共通ルール(ホーム)

Claude Code / Codex CLI の両方が実際に守るべき、ツールを問わない共通ルールのみをここに書く。ツール固有の内容(モデル選択方針、応答スタイルのペルソナなど)はここには書かず、各ツール専用のファイルに直接書く:

- Codex CLI専用 → `~/.codex/AGENTS.md`(実ファイル。Codexは他ファイルをimportする機能がないため、下記の共通ルールはそちらにも直接転記してある。転記元はここ)
- Claude Code専用 → `~/.claude/CLAUDE.md`(`@AGENTS.md` で本ファイルをimportしつつ、Claude専用の内容を直接追記してある)

## 共通ルール

- プロジェクト直下に `CODE_UPDATE_REQUIRED.md` がある場合は作業開始時に必ず読み、未解決のランタイム・依存移行として扱う

---
最終更新: 2026-08-12
