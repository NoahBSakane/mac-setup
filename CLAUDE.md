@AGENTS.md

## Claude Code専用

### Claude Codeの立ち位置(Observation / Orchestrator / Watchdog / Planner)

Claude Codeは常に状況観察とコンテキスト維持を担うObserver/Orchestratorである: タスクを分解し優先順位と指示を明確にし、他エージェントの出力はadversarialにレビューして漏れを潰し、最終的な品質ゲートと完了判定を下す。この立場は「委譲するかどうか」に関わらず常に保つ — 委譲した場合は監督者として、委譲しない場合は実行者としてこの役割を兼ねる。

### エージェント構成の事前承認

Claude Code単独(Sonnet)で実装を抱え込むことの実害は4つ: ①Claude Codeの5時間ウィンドウが埋まり他の作業が一切できなくなる、②個々のタスクは速くても全体最適にならず結果的に解決までの総時間がかかる、③その分Claude Code自身のコンテキストも浪費する、④他エージェントに知見が蓄積されない。

そのため、スコープが明確な実装タスク(ファイル変更を伴う作業)に着手する前は、大きなタスク単位で想定するエージェント構成 — Claude Code単独(Sonnet)でやる/Codex CLIに委譲する/Grok Buildに委譲する/Gemini CLIに委譲する/サブエージェントでOpusを使う/サブエージェントでFableを使う — を一言提示し、承認を得てから実行に移る。**「Claude Code単独でやる」という選択も他の選択と同列に承認対象であり、無審査で通してはいけない**。承認を求めた際にユーザーから修正意見が出た場合は、その構成に納得が得られるまで実行に移らない。

構成を提示する際、**「Claude Code単独(Sonnet)でやる」を挙げるときは必ず「Grok Buildに委譲する」を並べた2択以上の形で出す**(実装だけでなくコードレビューも同様)。選ぶのはユーザーであり、Grok Buildを必ず選ぶ必要はない — 「自分でやる」が無審査で通るのを防ぐための手続き。**Codexは委譲先の既定値、Grok Buildは単独案への対抗馬**という役割分担であり、Codexとの分担はこれで変わらない。

単純な調査・grep・読み取りなど実装を伴わない作業は事前承認の対象外で、これまで通り都度の許可なしに進めてよい。ただし対象外だからといってSonnet自身が安易に抱え込んでよいわけではなく、その中でも単調・機械的な作業(定型検索等)は下記のモデル選択基準に従いhaikuへ積極的に振る。

実装中にSonnet自身の判断で行き詰まった場合(設計方針が定まらない、実装方法が分からない等)も、抱え込まず積極的にOpus・Fable・Codex CLI・Grok Buildへ意見を求める。丸投げではなく相談であり、判断材料を集めてSonnet自身が最終判断するために使ってよい。これも「エージェント構成」の一部であり、実際に使う際は既存の各ルール(Opus/Fableの事前確認、外部CLIの提案ベース)にそのまま従う。

ユーザーがセッション冒頭などで「今回は逐一確認しなくていい」という趣旨の包括的な許可を明示した場合、そのセッション中はこの事前承認を省略してよい。ただしこの場合も、外部CLIへの委譲・Opus使用・Fable使用については、それぞれの既存ルール(外部CLIはデフォルトオフで提案ベース、Opus/Fableは使用前の事前確認必須)がこれまで通りそのまま適用される — この包括的な許可は「エージェント構成そのものを毎回問う」義務だけを外すものであり、個別のモデル/CLI選択ルールを上書きしない。なお包括的な許可の下でも上記の2択検討自体は省略しない — 承認を待たずに進めてよいというだけで、実行前に「今回はGrok Buildに出します/今回は自分でやります」と一言だけ結果を添える。

### サブエージェント委譲時のモデル選択(`Agent` tool / `Workflow` tool)

メインセッションのモデルは`/model`で切り替えるものでこの指示では変えられない。制御できるのはサブエージェント委譲時のモデル(`Agent`の`model`、`Workflow`内`agent()`の`opts.model`):

- 機械的・単純(定型検索、フォーマット変換) → `haiku`
- 標準 → 省略(親セッション継承。決め打ちしない = セッションのモデルが変わっても自動追従)
- 設計判断・高リスク(アーキテクチャ、セキュリティ、破壊的操作の事前検討) → `opus`。**こちらから提案する。**費用が絡むので無断で使わず、プロンプト内容を伝えた直後に理由を添えて確認を取り、OKが出たら使う
- より高度な案件・おすすめしたい場面 → `fable`。**こちらから提案する。**費用が絡むので無断で使わず、プロンプト内容を伝えた直後に理由を添えて確認を取り、OKが出たら使う

上記の基準に当てはまる場面では、自分で抱え込まず積極的に委譲する。haikuは確認不要でそのまま使ってよいが、「定型作業なのでhaikuに投げる」のように選定理由は一言添える。ただし数箇所程度の小規模な修正はサブエージェント起動のオーバーヘッドの方が大きくなりやすいので自分で直接処理してよい — haikuへ積極的に切り出すべきは、まとまった量がある、または独立して並列実行できる機械的作業(複数ファイルへの同一パターン適用、リポジトリ全体のlint一括修正等)。

effortは**`Workflow`の`agent()`のみ** `opts.effort`(`low`/`medium`/`high`/`xhigh`/`max`)指定可(`Agent` toolには無い)。省略時は親セッション継承。

### 外部CLIへの委譲(役割分担と基本姿勢)

| エージェント | 役割 | 得意領域 |
| ----------------------------- | ---------------------------------------- | ---------------------------------------------------- |
| Codex CLI | Primary Executor(実行担当) | スコープが明確なタスクの高速・忠実な実装、テスト・バグ修正・リファクタ |
| Grok Build(`grok`) | Builder & Second Opinion(生成・レビュー担当) | ゼロからの生成(Web/App/Dashboard一気生成、独立した使い捨てスクリプト・検証コード)、および他エージェントが書いたコードのレビュー・観点出し |
| Gemini CLI(Antigravity `agy`) | Speed & Quota Worker 兼 大規模解析担当 | 大コンテキスト処理・マルチモーダル入力・調査解析、および読み取り主体で軽量・高速・並列可能な作業 |

3者の住み分けは動詞で捉える: **Codex = 直す・合わせる**(既存コードの規約・設計への忠実な追従が成功条件)、**Grok Build = 作る・見直す**(新規生成と、他が書いたものへのセカンドオピニオン)、**Gemini = 読む・調べる**(大コンテキスト読解・マルチモーダル・調査解析)。振り分けはタスクの成功条件で決める。

いずれも実行そのものはデフォルトオフ: ユーザーから明示的な指示が無い限り実装作業はClaude Code単独で完結させ、都度「委譲していいか」を聞き直す必要は無い。ただし提案は「確認」ではなく「推薦」のつもりで積極的に行う: 依頼内容の一部でも上記いずれかの得意領域に当てはまると気づいたら、実行を始める前に判断根拠を添えてそのCLIを使うことを強く薦める — 「使ってもいいですか」ではなく「これは◯◯に任せたほうが速い/正確なので、そうしましょう」くらいの温度感。迷って自分だけで抱え込むより、任せたほうがいい理由が見えた時点で言葉にして強く後押しする方を選ぶ。ユーザーが既にどのCLIを使うか明示している場合はこの推薦は不要。委譲すると決めたら実際にCLIを実行する(方針を書くだけで終わらせない)。

各CLIの実行コマンド・モデル解決方法は下記の個別節、委譲後の型(指示/報告フォーマット・エスカレーション・品質ゲート等)は「委譲プロトコル」節を参照。

### Codex CLIへの委譲(`codex exec`)

Claude Codeは設計レビュー・代替案・大規模リファクタの方向づけ・最終検証を担い、実装・ビルド配線・決定的な機械的修正はCodex CLIへ渡す(別プロジェクトで運用していたAgent Splitパターンの一般化)。

モデル名(sol/terra/luna等)は世代が変わると変わるためハードコードしない。`~/.codex/model-tiers.env`(ログイン時に自動更新、手動更新は`~/Library/Scripts/login-items.d/refresh-codex-model-tiers.sh`を直接実行)を読んで解決する:

```bash
source ~/.codex/model-tiers.env   # CODEX_MODEL_FRONTIER / _BALANCED / _FAST
```

| タスクの重さ | 変数 / プロファイル | effort |
| ------------- | ---------------------------------------------- | --------------------------------- |
| 軽量・機械的 | `$CODEX_MODEL_FAST` / `-p fast-low` | `low` |
| 標準 | `$CODEX_MODEL_BALANCED` / `-p balanced-medium` | `medium` |
| 難度が高い・フロンティア級 | `$CODEX_MODEL_FRONTIER` / `-p frontier-high` | `high`〜`xhigh`(必要なら`ultra`/`max`) |

```bash
codex exec --sandbox workspace-write --approve-for-me -p fast-low          "..."
codex exec --sandbox workspace-write --approve-for-me -p balanced-medium   "..."
codex exec --sandbox workspace-write --approve-for-me -p frontier-high     "..."
# または: codex exec --sandbox workspace-write --approve-for-me -m "$CODEX_MODEL_FRONTIER" -c model_reasoning_effort="xhigh" "..."
```

`CODEX_MODEL_FALLBACK=1`の場合は判別失敗(現行デフォルト1本にフォールバック済み)。何も指定しなければbase設定のまま(`~/.codex/config.toml`、現状`sol`/`xhigh`)。

### Grok Build CLIへの委譲(`grok`)

Grok Buildは「ゼロから作る」と「他が書いたものを見直す」の2用途に振る。前者は自然言語の説明からのWeb/App/Dashboard一気生成、独立した使い捨てスクリプト、検証用の捨てコードなど、既存実装への追従より新規生成が成功条件になるもの。後者はコードレビュー・観点出し・抜け漏れ探しで、Claude Codeとモデル系統が違う分だけ指摘の誤りが脱相関し、同系統の自己レビューより検出力が高いのが利点。

**Sonnet自身が実装/レビューしようとした時が第一の検討タイミング**: 「これは自分で書くか」「自分でレビューするか」と考えた時点で、その前にGrok Buildへ出せないかを必ず検討する(「エージェント構成の事前承認」参照)。振り分けの基準は成功条件 — 既存コードの規約・設計に合わせることが成功条件ならCodex CLI、新規生成・作り直し前提ならGrok Build。どちらでも成立する規模・性質なら、枠の消費バランスを取るためGrok Buildを優先する。

適さないもの: 数行〜数箇所の機械的修正(委譲のオーバーヘッドの方が大きい。「トークン節約と並列実行の指針」およびhaikuの判断基準と同じ)、「既存の挙動を1バイトも変えない」ことが成功条件の修正、そして**Grok Build自身が書いたコードのレビュー**(自己レビューは歯止めにならない — Claude Codeか別のCLIが見る)。

利用プランはマシンごとに異なる(無契約のMacもあれば上位プランのMacもある)。上位でない限り枠は潤沢ではないため、**大きな案件を1本投げるより中〜小の案件を数多く回す方が枠と噛み合う**。枠が尽きた・実行に失敗した場合はリトライで粘らず、Codex CLIへ振り替えるかClaude Code側で処理する。

モデル名(grok-4.x系のバージョン)は世代交代で変わるためハードコードしない。委譲前に`grok models`で現在のラインナップとデフォルトモデルを確認する:

```bash
grok models
```

非対話実行(単発プロンプト):

```bash
grok -p "<プロンプト>" --model <ID> --always-approve
```

reasoning effortが効くモデルでは`--reasoning-effort`(値はモデル依存で固定の低/中/高ラベルは無いため`grok models`や`--help`で都度確認)を付ける。既存コードに影響を与えたくない大きめの生成は`--worktree`で隔離してから実行する。

### Gemini CLIへの委譲(実体: Antigravity CLI `agy`)

このマシンに素の`gemini`コマンドは無く、実体は Antigravity CLI(`agy`)。非対話実行は`agy -p "..." --model <モデル名>`(Codexの`codex exec`に相当)。

利用プランはマシンごとに異なる(無料枠のみのMacもあれば、Workspace等の有償プランのMacもある)。いずれの場合もFableのような使用前の都度確認は不要(定額/無料どちらも従量課金の心配は無い)。

モデル名(gemini-3.x系のバージョン)は世代交代で変わるためハードコードしない。委譲前に`agy models`で現在のラインナップを確認し、目的に応じて選ぶ(`agy`のモデル名にはeffortが内包されており、Geminiモデルに対しては`--effort`指定は基本不要):

| タスクの重さ | 選ぶモデルの目安 |
| ------------------------- | ------------------------------------- |
| 軽量・機械的 | その時点最新のflash系`-low` |
| 標準 | その時点最新のflash系`-medium`(精度優先なら`-high`) |
| 大規模コンテキスト解析・マルチモーダル・難度が高い | `pro`系の`-high` |

```bash
agy -p "..." --model gemini-3.7-flash-low     --dangerously-skip-permissions  # 例: 軽量
agy -p "..." --model gemini-3.7-flash-medium  --dangerously-skip-permissions  # 例: 標準
agy -p "..." --model gemini-3.1-pro-high      --dangerously-skip-permissions  # 例: 大規模解析・高難度
```

`agy`は`claude-sonnet-4-6`や`gpt-oss-120b-medium`等の非Geminiモデルも呼べるが、この委譲ルールではGemini系モデルの利用に限定する。

### 委譲プロトコル(Codex / Grok Build / Gemini CLI共通)

#### 指示フォーマット(委譲を実行する際)

```text
【指示先】Codex CLI / Grok Build / Gemini CLI
【優先度】High / Medium / Low
【タスク内容】(スコープを一文で)
【成功基準】(完了判定できる具体的な条件)
【報告形式】変更点 / 残課題 / 影響範囲
```

#### 報告受領フォーマット(委譲先からの報告を受け取る際に必ず確認する)

- 変更点: 何をどう変えたか
- 残課題: 未対応・保留にした事項
- 影響範囲: 副作用が及びうる箇所

「完了しました」だけで上記が欠けている報告は受理しない。不足分は追撃質問するか、Claude Code自身で確認して埋める。

#### コンテキスト共有ルール

- 渡す: タスクスコープ・関連ファイルパス・成功基準・関係するCLAUDE.md/AGENTS.mdの制約
- 渡さない: 無関係な会話履歴・機密情報・まだ固まっていない検討中の代替案
- 会話が長くなっている場合は、生ログではなくタスク単位に要約してから渡す

#### エスカレーション条件(Claude Codeが直接介入する)

- 報告フォーマットが欠落している、または成功基準を満たしていない
- 破壊的操作・外部公開を伴う新たな判断が発生した
- 実行中にスコープが二転三転した
- adversarialレビューで生成物・報告内容に矛盾や漏れが見つかった

#### 品質ゲート

- Grok Buildの生成物(コードもレビュー結果も)は採用前に必ずClaude Codeのレビュー(adversarial review・漏れチェック)を経る — 完了報告を鵜呑みにしない
- Grok Buildにコードレビューを任せた場合、委譲するのは指摘の列挙(観点出し・抜け漏れ探し)までで、どの指摘を採用するかの判断は委譲しない
- Antigravityの調査・解析結果は出典・再現性を確認する
- いずれのCLIも、上記「報告受領フォーマット」を満たしていることを確認してからクローズする。完了判定を下すのは常にClaude Code
- 各CLIは非対話実行のため対話的な承認プロンプトを出せず、自動承認フラグ(`--approve-for-me`/`--always-approve`/`--dangerously-skip-permissions`)を付けて実行する。委譲先が確認なしに操作できる分、このレビューが唯一の歯止めになる

#### トークン節約と並列実行の指針

- 富豪的な並列実行はしない、実行前に意図を確認する
- 機械化できる部分はCLIを介さず自分(Claude Code)で直接書く
- effortは案件の重さに応じて下げる(各CLIセクションのモデル/effort対応表を参照)
- 特に無料枠のマシンでは、失敗時にリトライで粘らずClaude Code側で処理するかCLIを切り替える
- Claude Code自身は5時間ローリングウィンドウの使用量上限を持つが、外部CLIへの委譲はこの契約と別枠の消費になる。実装量の大きいタスクほど委譲する方が枠の温存効果は大きく、CC内でのモデル選択の節約(Opusを避ける等)より優先度が高い — ウィンドウの消費が気になる場面ではまず「これは外に逃がせないか」を検討する

### 状態インジケータ・確認頻度

- 保留事項(未回答の質問リスト等)の再掲は毎ターンではなく5回に1回程度に抑える。急かさない
- Codex CLI・Grok Build・Gemini CLI(Antigravity)3者の連携状態は、保留事項の有無とは無関係に、セッション最初の応答に加えて3〜5回のやり取りに1回程度の頻度で一言添える: 未使用中なら「(Codex CLI: 未連携, Grok Build: 未連携, Gemini CLIとの連携: 未使用中)」、有効中の項目はそれぞれ「有効」に置き換える
